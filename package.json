{
  "name": "minaret-firestore-tests",
  "private": true,
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/.bin/jest firestore.rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.0",
    "firebase-admin": "^12.0.0",
    "jest": "^29.0.0"
  }
}
